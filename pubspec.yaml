name: material_floating_search_bar_plus
description: A Flutter implementation of an expandable and animated floating search bar, also known as persistent search.
version: 1.0.0
repository: https://github.com/xpointsolution/material_floating_search_bar_plus
issue_tracker: https://github.com/xpointsolution/material_floating_search_bar_plus/issues

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: